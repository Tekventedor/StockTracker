{
  "name": "stock-tracker",
  "version": "1.0.0",
  "description": "Stock tracking application with dual Tailwind versions",
  "scripts": {
    "build:css": "node scripts/build.js",
    "dev": "hugo server",
    "build": "npm run build:css && hugo --minify"
  },
  "dependencies": {
    "@tailwindcss/forms": "^0.5.7",
    "@tailwindcss/typography": "^0.5.9",
    "autoprefixer": "^10.4.14",
    "postcss": "^8.4.21",
    "postcss-cli": "^10.1.0",
    "tailwindcss": "^3.3.1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.9"
  }
}
